function xxx_xxx(param){
    /* to be generated
    const {age,level,interest,interests,profession} = param;
    */
    return (
    /**start**/
    (age > 18) ||
    (age < 18 + 12) && 
    (level ==='3') && 
    (interests.includes('出国游'))&& 
    (['出国游','看电影'].includes(interest))||
    !(profession === '上班族')
    /**end**/
    )
}